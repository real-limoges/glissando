[project]
name = "rust"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.1",
    "polars>=1.37.1",
    "pyarrow>=23.0.0",
]
